import React, { useState, useEffect } from 'react';
import { SafeAreaView, StyleSheet, Text, View, Dimensions, Alert, ActivityIndicator, BackHandler } from 'react-native';
import _ from 'lodash';
import SendSMS from 'react-native-sms';

const ServiceIn = () => {
  return (
    <View>
      <Text>입고</Text>
    </View>
  );
};

export default ServiceIn;
